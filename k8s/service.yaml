apiVersion: v1
kind: Service
metadata:
  name: fcg-users-api
spec:
  selector:
    app: fcg-users-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001
  
  type: NodePort